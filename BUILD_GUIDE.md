# 保研助手桌面应用 - 构建指南

## 📦 文件说明

本次交付包含以下内容：

1. **`baoyan-helper-source.zip`** - 完整的桌面应用源代码
2. **`dist/`** - 已构建的前端文件（可直接在浏览器中打开）

## 🚀 快速开始

### 方式一：直接使用 Web 版本（无需构建）

1. 解压 `dist.zip`
2. 双击 `index.html` 用浏览器打开
3. 即可使用完整功能

### 方式二：构建桌面应用（推荐）

#### 环境准备

- **Node.js**: 18.0 或更高版本
- **npm**: 9.0 或更高版本

#### 构建步骤

1. **解压源代码**
   ```bash
   unzip baoyan-helper-source.zip -d baoyan-helper
   cd baoyan-helper
   ```

2. **安装依赖**
   ```bash
   npm install
   ```

3. **开发模式运行**
   ```bash
   npm run electron:dev
   ```

4. **构建桌面应用**

   - **Windows (.exe 安装包)**:
     ```bash
     npm run electron:build:win
     ```
     输出: `release/保研助手 Setup 1.0.0.exe`

   - **macOS (.dmg 安装包)**:
     ```bash
     npm run electron:build:mac
     ```
     输出: `release/保研助手-1.0.0.dmg`

   - **Linux (.AppImage 和 .deb)**:
     ```bash
     npm run electron:build:linux
     ```
     输出: `release/保研助手-1.0.0.AppImage` 和 `.deb`

## 📁 项目结构

```
baoyan-helper/
├── dist/                    # 前端构建输出（已构建好）
│   ├── index.html
│   └── assets/
├── dist-electron/           # Electron 主进程代码
│   ├── main.js             # 主进程入口
│   └── preload.js          # 安全预加载脚本
├── build/                   # 应用图标
│   └── icon.png
├── src/                     # 前端源代码
│   ├── components/          # React 组件
│   ├── pages/               # 页面组件
│   │   ├── Dashboard.tsx   # 总览页
│   │   ├── Projects.tsx    # 项目管理
│   │   ├── ProjectDetail.tsx # 项目详情
│   │   ├── Notifications.tsx # 通知推送
│   │   ├── Assistant.tsx   # 智能助手
│   │   ├── Interview.tsx   # 模拟面试
│   │   └── Schedule.tsx    # 日程管理
│   ├── db/                  # 数据库
│   │   └── database.ts     # IndexedDB 封装
│   ├── types/               # TypeScript 类型
│   │   └── index.ts
│   ├── utils/               # 工具函数
│   │   └── electron.ts     # Electron API 封装
│   ├── App.tsx             # 应用入口
│   └── main.tsx            # React 入口
├── package.json            # 项目配置
├── vite.config.ts          # Vite 配置
├── tsconfig.json           # TypeScript 配置
└── README.md               # 项目说明
```

## 🛠️ 技术栈

| 技术 | 用途 |
|------|------|
| React 19 | 前端框架 |
| TypeScript | 类型安全 |
| Vite | 构建工具 |
| Tailwind CSS | 样式框架 |
| shadcn/ui | UI 组件库 |
| Electron | 桌面应用框架 |
| Dexie.js | IndexedDB 封装 |
| date-fns | 日期处理 |

## ✨ 功能特性

### 1. 项目管理
- 添加/编辑/删除保研项目
- 预设主流院校项目模板
- 项目状态追踪（准备中/已提交/面试中/已录取）

### 2. 材料管理
- 10类材料分类存储：
  - 成绩单、排名证明、英语证明
  - 推荐信、个人陈述、简历
  - 论文成果、实习实践、复习材料、其他
- 支持 PDF、Word、图片等多种格式
- 文件上传/下载/删除

### 3. 通知推送
- 自动监控项目官网
- 新通知实时推送
- 一键跳转到官网

### 4. 智能助手
- AI 驱动的问答系统
- 保研流程咨询
- 项目信息查询

### 5. 模拟面试
- 综合面试模拟
- 专业面试模拟
- 英语面试模拟
- 实时反馈建议

### 6. 日程管理
- 周视图/列表视图
- 任务和里程碑
- 进度追踪

## 🔒 数据安全

- **本地存储**: 所有数据保存在用户设备上
- **无需注册**: 打开即用，无需账号
- **IndexedDB**: 浏览器内嵌数据库，数据持久化
- **桌面端数据目录**: `~/.baoyan-helper/`

## 🐛 常见问题

### Q: 安装依赖时卡住？
A: 尝试使用淘宝镜像:
```bash
npm config set registry https://registry.npmmirror.com
npm install
```

### Q: 构建时提示缺少 Python？
A: Electron 构建需要 Python 环境，请安装 Python 3.8+:
- Windows: 从官网下载安装
- macOS: `brew install python`
- Linux: `sudo apt install python3`

### Q: macOS 构建提示需要签名？
A: 开发测试可以跳过签名:
```bash
npm run electron:build:mac -- --publish=never
```

## 📄 许可证

MIT License

## 🤝 反馈建议

如有问题或建议，欢迎反馈！
